<h1 class="mb-4"><%= @page.title %></h1>

<div class="lead my-3"><%= sanitize @page.body %></div>

<%= nested_pages @pages.arrange(order: :name) %>

<%= link_to 'Edit page', "/#{page_path(@page)}/edit", class: 'btn btn-secondary mt-5' %>

<%= link_to 'Add page', "/#{page_path(@page)}/add", class: 'btn btn-secondary mt-5' %>
