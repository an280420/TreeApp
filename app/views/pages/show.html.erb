<h1 class="mb-4"><%= @page.title %></h1>

<div class="lead my-3"><%= sanitize @page.body %></div>

<%= render 'tree', page: @page %>

<%= link_to 'Edit page', "#{@page.name}/edit", class: 'btn btn-secondary mt-5' %>

<%= link_to 'Add page', "#{@page.name}/add", class: 'btn btn-secondary mt-5' %>
